services:
    Sengped_Admin:
      image: dpage/pgadmin4
      container_name: Sengped_Admin
      ports:
        - ${ADMIN_PORT}:80
      environment:
        PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
        PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASS}